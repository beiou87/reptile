{
  "name": "pwt_reptile",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:dist": "rimraf dist",
    "start": "yarn clean:dist && node app/index.js"
  },
  "author": "taodingyan",
  "license": "ISC",
  "devDependencies": {
    "osmosis": "^1.1.4",
    "rimraf": "^2.6.1"
  }
}
